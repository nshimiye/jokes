import jokeGenerator from '../../../utils/joke-generator';
import { module, test } from 'qunit';

module('Unit | Utility | joke generator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = jokeGenerator();
  assert.ok(result);
});
